/// @description  com_setup_observe(observable, observer)
/// @param observable
/// @param  observer

/*
var observable = argument0
var observer = argument1

add_observer(observable, observer)
on_destroy(building, cmd_create(remove_observer, game_controller.state_change, building))
*/
